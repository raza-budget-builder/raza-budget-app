// Single source of truth for the landing page's section width — 1200px,
// wider than the app's own max-w-5xl (1024px) convention used elsewhere,
// matching code-x.ai's container. Deliberate for this page only.
export const CONTAINER_CLASS = "mx-auto w-full max-w-[1200px] px-4 sm:px-8";
